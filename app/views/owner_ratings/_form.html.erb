<%= form_with(model: owner_rating, local: true) do |form| %>
  <% if owner_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(owner_rating.errors.count, "error") %> prohibited this owner_rating from being saved:</h2>

      <ul>
      <% owner_rating.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="container my-4">
    <h3>Rating for Owner</h3>
    <p>
      <strong>Owner</strong>
      <%= @owner_rating.owner.email %>
    </p>
    <div class="form-group col-md-6">
      <%= form.label :owner %>
      <%= form.text_field :owner, id: :owner, class: "form-control" %>
    </div>
    <div class="form-group col-md-6">
      <%= form.label :rating %>
      <%= form.text_field :rating, id: :rating, class: "form-control" %>
    </div>
    <div class="form-group col-md-6">
      <%= form.label :comment %>
      <%= form.text_field :comment, id: :comment, class: "form-control" %>
    </div>
  </div>
  
  <div class="container col-md-4 col-offset-md-4">
    <div class="actions">
      <%= form.submit class: "btn btn-primary btn-block" %>
    </div>
  </div>

<% end %>
